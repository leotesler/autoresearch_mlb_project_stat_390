---
title: "README"
output: html_document
---

Subdirectories:

`misc_files/`: Manually saved iteration models and experiment result processing.

R Scripts:

`model.R`: Agent-edited recipe and model specification, currently set to best performing iteration 60 state.

`prepare.R`: Specifications for model evaluation and result logging.

`run.R`: Calls the other two scripts and provides information to be logged during agent loops and experiments.

Markdown documents:

`program.md`: Written instructions and constraints for the AI agent to follow.
